### This file was generated by Nexus Schema
### Do not make changes to this file directly

type Mutation {
  creaftDraft(authorEmail: String!, content: String, title: String!): Post!
  deleteDraft(id: ID!): Post!
  updateDraft(authorEmail: String!, content: String, id: ID!, title: String!): Post!
}

type Post {
  author: User!
  authorId: String!
  content: String
  createdAt: String!
  id: ID!
  published: Boolean!
  title: String!
}

type Profile {
  bio: String
  id: ID!
  user: User!
  userId: String!
}

type Query {
  post(id: ID): Post!
  posts: [Post!]!
  profile: [Profile!]!
  users: [User!]!
}

type User {
  email: String!
  id: ID!
  name: String
  posts: [Post!]!
  profile: Profile
}
